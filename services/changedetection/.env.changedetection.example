# ============================
# ChangeDetection Environment Example
# ============================
# Copy to .env.changedetection and adjust as needed.

# Web UI port (host port mapped to container port 5000)
CHANGEDETECTION_PORT=5000

# Subdomain for Traefik (default: changedetection)
# Service will be accessible at: changedetection.${BASE_DOMAIN}
CHANGEDETECTION_SUBDOMAIN=changedetection

# JavaScript rendering via bundled sockpuppet browser (Playwright protocol)
PLAYWRIGHT_DRIVER_URL=ws://browser-sockpuppet-chrome:3000

# System default content fetcher
# html_webdriver enables screenshots/JS rendering by default.
DEFAULT_FETCH_BACKEND=html_webdriver

# Base URL added to notifications (use your public URL)
# BASE_URL=https://changedetection.example.com

# Respect reverse-proxy forwarded headers (recommended behind Traefik)
# USE_X_SETTINGS=1

# Optional tuning
# FETCH_WORKERS=10
# MINIMUM_SECONDS_RECHECK_TIME=3
# HIDE_REFERER=true
# DISABLE_VERSION_CHECK=true

# Optional sockpuppet browser tuning (used by browser-sockpuppet-chrome)
# SCREEN_WIDTH=1920
# SCREEN_HEIGHT=1080
# SCREEN_DEPTH=24
# MAX_CONCURRENT_CHROME_PROCESSES=10
