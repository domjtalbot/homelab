# Tdarr Environment Variables

# Web UI port (external port mapped to container)
TDARR_WEB_PORT=8265

# Server port for node communication
TDARR_SERVER_PORT=8266

# Subdomain for Traefik (default: tdarr)
# Service will be accessible at: tdarr.${BASE_DOMAIN}
TDARR_SUBDOMAIN=tdarr
