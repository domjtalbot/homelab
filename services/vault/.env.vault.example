# ============================
# Vault Environment Example
# ============================
# Production mode with Raft storage. See https://developer.hashicorp.com/vault/docs

# Web UI port (external host port mapped to container port 8200)
VAULT_PORT=8220

# Subdomain for Traefik (default: vault)
# Service will be accessible at: vault.${BASE_DOMAIN}
VAULT_SUBDOMAIN=vault

# Public API address (used by Vault UI and clients)
VAULT_API_ADDR=https://vault.example.com

# Cluster address used for Raft communication
VAULT_CLUSTER_ADDR=http://vault:8201

# Log level (trace, debug, info, warn, error)
VAULT_LOG_LEVEL=info

# Enable Vault UI (true/false)
VAULT_UI=true

# --- Backup Configuration (container paths) ---
# Backups are stored in /backups inside the container.
VAULT_BACKUP_DIR=/backups

# Cron status JSON is stored in /cron-status inside the container.
CRON_STATUS_DIR=/cron-status

# --- Backup Configuration (host paths) ---
# Host path for Vault snapshot backups
VAULT_BACKUP_HOST_DIR=/path/to/vault-backups

# Host directory where cron status JSON files are written
VAULT_CRON_STATUS_HOST_DIR=/path/to/cron-status

# Token with permission to read raft snapshots
VAULT_BACKUP_TOKEN=changeme_create_token

# Retention settings (per tier)
VAULT_BACKUP_DAILY_RETENTION=7
VAULT_BACKUP_WEEKLY_RETENTION=4
VAULT_BACKUP_MONTHLY_RETENTION=6
