# =============================
# Linkwarden Environment Example
# =============================
# See https://docs.linkwarden.app/self-hosting/installation for full documentation

# Web UI port (external host port mapped to container port 3000)
LINKWARDEN_WEB_PORT=3003

# Subdomain for Traefik (default: linkwarden)
# Service will be accessible at: linkwarden.${BASE_DOMAIN}
LINKWARDEN_SUBDOMAIN=linkwarden

# --- Required Configuration ---

# NextAuth configuration
# URL where Linkwarden will be accessible (used for authentication callbacks)
NEXTAUTH_URL=https://linkwarden.home.example.com/api/v1/auth

# Secret for NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=changeme_generate_a_secret

# PostgreSQL password (generate with: openssl rand -base64 32)
POSTGRES_PASSWORD=changeme_generate_a_password

# MeiliSearch master key (generate with: openssl rand -base64 32)
MEILI_MASTER_KEY=changeme_generate_a_key

# --- Optional Configuration ---

# Disable new user registration (default: false)
# NEXT_PUBLIC_DISABLE_REGISTRATION=false

# Enable credentials-based authentication (default: true)
# NEXT_PUBLIC_CREDENTIALS_ENABLED=true

# Maximum links per user (default: unlimited)
# MAX_LINKS_PER_USER=

# Storage folder inside container (default: /data/data)
# STORAGE_FOLDER=/data/data

# Browser timeout for archiving in milliseconds (default: 60000)
# BROWSER_TIMEOUT=60000

# --- AI Configuration (Optional) ---

# Ollama endpoint for local AI tagging
# NEXT_PUBLIC_OLLAMA_ENDPOINT_URL=http://localhost:11434
# OLLAMA_MODEL=llama2

# OpenAI API key for AI features
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-3.5-turbo

# --- SSO Configuration (Optional) ---

# GitHub OAuth
# NEXT_PUBLIC_GITHUB_ENABLED=true
# GITHUB_ID=
# GITHUB_SECRET=

# Google OAuth
# NEXT_PUBLIC_GOOGLE_ENABLED=true
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Keycloak
# NEXT_PUBLIC_KEYCLOAK_ENABLED=true
# KEYCLOAK_ISSUER=
# KEYCLOAK_CLIENT_ID=
# KEYCLOAK_CLIENT_SECRET=

# --- SMTP Configuration (Optional) ---

# Email provider for password reset, etc.
# NEXT_PUBLIC_EMAIL_PROVIDER=true
# EMAIL_FROM=noreply@example.com
# EMAIL_SERVER=smtp://user:password@smtp.example.com:587
