# =================================
# Open WebUI Environment Variables
# =================================

# Web UI port (external port mapped to container)
OPEN_WEBUI_WEB_PORT=3000

# --- Required Backend Configuration ---
# URL to your Ollama server (required for Ollama integration)
# Default: (no default, must be set for Ollama)
OLLAMA_BASE_URL=http://host.docker.internal:11434

# --- Authentication & User Management ---
# Enable authentication (default: True)
# WEBUI_AUTH=true

# Allow new users to sign up (default: True)
ENABLE_SIGNUP=false

# Default role for new users (default: user)
# DEFAULT_USER_ROLE=user

# Secret key for session encryption (set to a random string in production)
# Default: (no default)
# WEBUI_SECRET_KEY=

# --- Database & Storage ---
# Custom database URL (default: internal SQLite)
# DATABASE_URL=sqlite:///app/backend/data/db.sqlite3

# --- Offline Mode ---
# Prevent HuggingFace downloads (default: 0)
# HF_HUB_OFFLINE=0

# --- OpenAI API (if not using Ollama) ---
# OPENAI_API_KEY=your_openai_api_key

# --- Other Advanced Options ---
# Add any additional variables as needed per documentation
