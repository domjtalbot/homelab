# MeTube Environment Variables

# Web UI port (external port mapped to container)
METUBE_WEB_PORT=8085

# Subdomain for Traefik (default: metube)
# Service will be accessible at: metube.${BASE_DOMAIN}
METUBE_SUBDOMAIN=metube
