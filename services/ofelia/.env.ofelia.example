# ============================
# Ofelia Scheduler Environment Example
# ============================

# Timezone used for cron schedules (e.g., UTC, America/Los_Angeles)
OFELIA_TZ=UTC

# Subdomain for the cron status JSON endpoint
# Status will be accessible at: https://cron-status.${BASE_DOMAIN}
OFELIA_STATUS_SUBDOMAIN=cron-status

# Port used by the status HTTP server inside the container
OFELIA_STATUS_PORT=8090

# Host directory where cron status JSON files are written
CRON_STATUS_HOST_DIR=/path/to/cron-status
